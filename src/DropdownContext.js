import React from 'react';

export default React.createContext({
  setMenuElement() {},
  setToggleElement() {},
  onClose() {},
  onToggle() {},
  popper: {},
  toggleId: undefined,
  show: null,
  alignRight: null
});
